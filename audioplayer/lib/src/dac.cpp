#include <dac.h>

void DAC_Init() {
	//Enable DAC channel by setting ENx in control register

	//enable dac output buffer to drive output without external amp

	//Set data format - 8 bit right, 12 bit left or right.
}